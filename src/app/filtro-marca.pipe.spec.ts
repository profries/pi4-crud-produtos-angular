import { FiltroMarcaPipe } from './filtro-marca.pipe';

describe('FiltroMarcaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMarcaPipe();
    expect(pipe).toBeTruthy();
  });
});
